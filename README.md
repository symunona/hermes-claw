# hermes-crab

A paired mobile+web auth and realtime messaging prototype.

## Structure

- `app/` - Capacitor mobile app (pairing + passkey auth)
- `server/` - Backend API + WebSocket gateway
- `plans/` - Architecture and protocol specs
